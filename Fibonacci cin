#include <iostream>
using namespace std;
int main() {
  int a[10], x;
  cin >> a[0] >> a[1];
  for(int i=0;i<=9;i++)
  {
     a[i+2] = a[i] + a[i+1];
  }
  for(int i=0;i<=9;i++)
  {
    cout << a[i] << ' ';
  }
  
  return 0;
}
